export const reducer = (state, action) => {
    switch (action.type) {
        case "Action1":

            break;
        case "Action":
            break;
        default:
            return state;
    }
};

